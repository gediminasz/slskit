[tool.poetry]
name = "salinity"
version = "0.0.1"
description = ""
authors = ["Gediminas Zlatkus <gediminas.zlatkus@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
pyyaml = "^5.1"
jsonschema = "^3.2.0"
funcy = "^1.14"
salt = "^2019.2"

[tool.poetry.dev-dependencies]
pytest = "^5.0"

[tool.poetry.scripts]
salinity = 'salinity:run_module'

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
