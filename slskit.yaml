salt:
  fileserver_backend:
    - roots
    - gitfs
  file_roots:
    base:
      - tests/project/salt
      - tests/project/mock
  gitfs_remotes:
    - https://github.com/saltstack-formulas/ufw-formula.git
  pillar_roots:
    base:
      - tests/project/pillar

slskit:
  roster:
    tester:
      grains:
        os: Ubuntu
    tester-gitfs:
